================================================================
STATUS: all original requests are now DONE & verified (tsc + next build
compile/lint pass; runtime page-data step still needs Firebase env — pre-existing).
================================================================

COMPLETED
---------
- Tab bar: icon-only tabs, label in tooltip (no horizontal scrollbar).
- Tab action bar: icon + label on wide screens, icon-only + tooltip on narrow.
- Playground: dedicated /playground page — in-browser JS (Web Worker) + Python
  (lazy Pyodide/WASM). Nav icon in the top bar.
- Dashboard: /dashboard — per-user stats, GitHub-style streak heatmap, adaptive
  spaced-repetition ("Due for review"), domain radar. Nav icon in the top bar.
  Deep-links to topics via /?topic=<id>.
- Kanban: /kanban — dnd-kit board (Backlog -> To Do -> In Progress -> Done),
  card CRUD, priority, due dates (overdue highlight), per-user persistence via a
  new "kanban" section in /api/progress. Nav icon in the top bar.
- Shared: lib/progress-metrics.ts (metrics + spacedRepetition + buildHeatmap),
  lib/code-runner.ts (JS worker + Pyodide), components/center-loader.tsx.
- lastReviewed timestamps are tracked/persisted per topic (feeds spaced repetition).

OPTIONAL FUTURE IDEAS (not requested — nice-to-haves)
----------------------------------------------------
- Playground: stdin support (Pyodide can wire sys.stdin); shareable snippet links.
- Dashboard: per-day activity intensity in the heatmap (needs per-day activity
  counts; today loginDates is binary present/absent).
- Kanban: labels/tags, card search/filter, WIP limits, due-date sorting,
  archive for Done.
- Consider real syntax highlighting in the playground editor (overlay technique
  or a lightweight lib) — currently a plain monospace textarea.

DEPLOY / RUNTIME NOTES
----------------------
- Requires .env.local (see .env.local.example): FIREBASE_* + SESSION_SECRET.
  Without them, `next build` fails at "Collecting page data" and the app can't
  run — this is environment config, not a code issue.
- Pyodide loads from cdn.jsdelivr.net on first Python run (playground / code tab).

WORKFLOW REMINDERS
------------------
- Never commit or push. At each checkpoint: verify ship-ready, then `git add` ONLY.
  (This repo has a Bash(git add:*) DENY rule, so the user stages manually.)
- Master is protected — branch before any commit.
- Files to stage for this session:
    git add app/page.tsx app/api/progress/route.ts app/dashboard app/kanban \
            app/playground components package.json package-lock.json
